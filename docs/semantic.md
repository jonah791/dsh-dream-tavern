# dsh-dream-tavern（梦境酒馆）· 语义文档 v0.3

> **状态**：`verified`（判据 A1–A7 全部已实测，§6 无 pending）｜**owner**：爱丽丝｜**日期**：2026-09-22
> **许可立场**：**全部自持**（主人 2026-09-22 定调）。**独立实现**，不 fork `flizzywine/dsh-tavern`（AGPL-3.0）。只借鉴**接口、行为与文件格式**，不复制其源码片段。

---

## 1. 定位与反定位

### 1.1 定位（是什么）
运行在 DSH 上的**文字游戏 / 角色扮演创作与游玩宿主**，两个并列目的：

1. **娱乐**：导入或制作人物卡，自由游玩或沿剧本推进。
2. **研究**：作为**上下文编排的可测量实验台**——「上下文对模型表现的影响」这一命题的受控实验载体。

### 1.2 反定位（不是什么）
- **不是**从零写的 agent 运行时：会话、模型路由、重试、压缩、工具、权限、Web GUI **一律复用 DSH 原生**。
- **不是** SillyTavern 的移植或兼容层：**导出/导入 ST 卡格式**是互操作需求，不是架构目标。
- **不是**通用聊天前端：没有卡与装配规则就没有本能力。
- **不是**评测框架本身：它**产出证据**，评分与统计归独立的测量装置（见 §5.4）。

### 1.3 与上游的关系（自持边界）
| 层面 | 可以做 | 不可以做 |
|---|---|---|
| 文件格式（ST 卡 PNG/JSON、世界书 JSON） | 读、写、往返 | — |
| **行为/接口**（候选与正文分离、后台结算、按需读上下文） | 借鉴并自己实现 | — |
| **源码** | 只读用于判断「有没有这条路」 | **不复制任何代码片段** |
| 分发 | 自己的仓库、自己的许可 | 不把上游代码并进自研仓库 |

---

## 2. 术语

- **卡（card）**：人物设定 + 开场 + 对话样例 + 正则 + 展示模板。
- **世界书（lorebook）**：按关键词/位置触发的条目集合。
- **预设（preset）**：上下文装配规则（槽位、顺序、触发、后处理）。
- **剧本（script）**：主线推进用的分段原文与进度锚点。
- **游玩会话（play session）**：一次游戏，含正文历史 + 后台状态。
- **装配单（assembly manifest）**：一次请求的**结构化描述**——每个片段来自哪个槽位、什么触发条件命中、注入在哪个深度、逐字节内容与哈希。**本能力的核心产物。**
- **后台结算（settlement）**：只改状态、不产生正文的独立 Agent 调用。
- **候选（candidate）**：与正文分开生成的若干行动选项。

---

## 3. 概念模型与不变量

### 3.1 三个 Agent，职责互斥
| Agent | 产出 | 写权限 |
|---|---|---|
| 正文 Agent | 叙事正文 | 只读状态 |
| 候选 Agent | N 个行动选项 | 只读状态 |
| 后台结算 Agent | 状态变更（结构化） | **唯一**状态写者 |

### 3.2 装配位置模型
槽位 = `system` / `persona_prefix` / `persona_suffix` / `before_history` / `depth-N`（倒插历史第 N 条之后）/ `after_history`；每条注入项带：来源、触发条件、优先级、是否可缓存。

### 3.3 不变量（违一则能力不成立）
1. **装配单即事实**：装配单逐字节重建出的请求体，必须等于实际发出的请求体。
2. **状态单写者**：只有后台结算 Agent 可改状态；正文与候选对状态只读。
3. **状态与正文同生共死**：回退必须原子——正文与状态一并还原到同一点。
4. **只追加**：已发出的历史前缀不得重写（缓存友好）；任何「重写历史」的设计须显式标注为破坏缓存。
5. **确定性**：同一输入 + 同一卡/预设/世界书 ⇒ 装配单哈希一致（模型采样除外）。

---

## 4. 契约（含调用点清单）

> ⚠ 本条目要求**契约必须写调用点**（§5.20 规则 3：只说「谁做什么」会漏点）。

### 4.1 存储布局
```
<dataDir>/
  cards/<cardId>/{card.json, source.png?, assets/}
  lorebooks/<id>.json
  presets/<id>.json
  scripts/<id>.json
  sessions/<sessionId>/{history.jsonl, state.json, manifests/<turn>.json}
```

### 4.2 工具面（宿主侧，初步）
| 工具名 | 意图 | 调用点 |
|---|---|---|
| `tavern_card_list` / `_read` / `_write` | 卡库读写 | 卡管理 UI、游玩会话启动、卡编辑 Agent |
| `tavern_lorebook_match` | 世界书触发匹配（纯函数） | 装配器（**唯一**调用点）、单测 |
| `tavern_assemble` | 产出装配单 | 正文/候选/后台三条路径**共用** |
| `tavern_state_apply` | 应用状态变更 | **仅**后台结算 Agent |
| `tavern_rollback` | 原子回退 | 游玩 UI、命令 |

### 4.3 装配器契约
`assemble(card, lorebook, preset, script, history, state, turnInput) -> { manifest, messages }`
- 纯函数、无 IO（IO 由调用方先取好喂入）⇒ **可离线单测**。
- `manifest` 含：`entries[]`（slot, source, triggerHit, depth, bytes, sha256）、`totalChars`、`hash`。

### 4.4 已识别的调用点风险（来自 §5.20 的事故史）
- 装配路径有**三条**（正文/候选/后台）⇒ 必须**共用** `tavern_assemble`；历史上「面板 3 种接入只改 1 种」「守护 3 条拉起路径只改 1 条」都出在这里。

---

## 5. 边界与信任

1. **卡与世界书是不可信输入**（第三方卡含 HTML/正则/脚本）⇒ 展示层隔离；正则与模板**不得**取得工具权限。
2. **卡内脚本不执行**：只做声明式解析；需要执行的一律走宿主已审核的扩展点。
3. **研究数据的边界**：装配单与请求往返属**我方的**取证数据；第三方案例内容按来源标注，不外发。
4. **测量装置独立**（自有许可，见 §1.3）：评分器、统计、报告**不并入本插件**，只读其产物格式。

---

## 6. 可证伪验收

> 跑法：`node scripts/acceptance.mjs`（一条命令出判据表 + 真数据读数 + A7 旁路面）。
> 退出码：`0` 全量通过 ｜ `1` 判据失败 ｜ `2` 判据通过但真数据不可达（PASS 降级）。

| # | 判据（可一次测量判真假） | 状态 | 实测证据（2026-09-22） |
|---|---|---|---|
| A1 | 装配单重建的请求体与实际请求 **逐字节相等** | **已实测** | 单测 3 例（1:1 / 篡改 / 少发）；真模型轮连续通过，`a1Ok:true` |
| A2 | 同输入两次运行 ⇒ 装配单 `hash` 一致 | **已实测** | 单测 2 例（同 base 复跑 / 跨会话同输入同 hash） |
| A3 | 回退后正文与状态**字节级**等于快照 | **已实测** | 单测 2 例 + **行为级重放**（回退后重跑同轮得同装配单）；反例：无快照则拒绝且零改动 |
| A4 | 缓存命中可读，且与「只追加」相符 | **已实测** | `bash scripts/cache-experiment.sh <会话> 6 <卡id>`：连跑多轮，全轮 A1 真；`requestChars` 单调不减；`cacheRead` 单调不减；**命中比不塌**（见下表） |
| A5 | ST 卡往返不丢字段 **且不改卡** | **已实测** | **主人 58 张真卡全量通过**（键集与值逐一比对 + PNG 字节往返） |
| A6 | 世界书匹配为纯函数 | **已实测** | 单测 10 例（含 depth/位置/优先级/确定性 probability/scanDepth） |
| A7 | 各路径共用装配器（无旁路） | **已实测** | **行为级**：工具路径与面板路径产出同一 manifest hash；静态：`src/` 下无其它 `assemble(` 调用点 |

**真实数据读数（2026-09-22）**：卡库 **58 张 PNG（112.4 MB）** —— 有开场 58 / 含卡内世界书 57 / 完全空 0；
**58 张全部可装配**（请求字数 min 1387 / max 24036 / 平均 15763；其中 44 张的实质内容由卡内世界书承载）。
世界书 **67 本（37.5 MB）** —— 解释 3442 条 / 留档 3659 条 / 跳过 217 条 / **已知未建模 20 种 / 未知 0 种**。
判据测试 **52/52**、`tsc` 0 错。

**A4 实验（2026-09-22 · 修复「世界书重复注入」后重跑，读数为干净值）**：
| 轮 | 请求字数 | 新增 tok | 缓存读 tok | 命中比 | A1 |
|---|---|---|---|---|---|
| 2 | 3986 | 219 | 896 | 80.4% | ✓ |
| 4 | 4477 | 425 | 1024 | 70.7% | ✓ |
| 6 | 4935 | 367 | 1408 | 79.3% | ✓ |
| 8 | 5474 | 494 | 1664 | 77.1% | ✓ |
| 10 | 6018 | 497 | 2048 | 80.5% | ✓ |

⇒ **「只追加 ⇒ 缓存命中率不塌」成立**（命中比在 70–81% 区间波动，无下降趋势）。
> 另有一次**修复前**的跑（`requestChars` 3986→10153、命中比 80.4%→83.5%）方向相同，
> 但其绝对值被重复注入撑大 ⇒ **不作为引用读数**，仅记此处以示两次独立跑一致。

⚠ **诚实边界**：命中比按服务端 `usage` 原样计算（`cacheRead / (cacheRead + input)`），**未核对该 provider 的字段口径**；
上游自称「缓存命中率 95%+」为**未核读数**，与我方链路不同源，不构成对照。

> 七条判据现均达「已实测」。`verified` 另要求 `pending == 0`（§5.20 规则 4）——本文件 §6 已无 pending。

---

## 7. 与实现关系

- **文档先行**：v0.1 草案 → v0.2（实现落地后按实践回修，见 §8）。
- 预设工程**已有正本**：`tavern/色欲之罪预设/`（V3.4 已发布，tag `v3.4`）+ 技能 `tavern-preset-iteration` + 插件 `dsh-preset-ops`（5 工具）。本能力**复用**它们，不重造。
- 语料与文风资产：`tavern/文风/`、`tavern/剧本素材/`、`tavern/素材/`。
- **卡库实体在主人的 TauriTavern 数据根**（只读）：`%APPDATA%/com.tauritavern.client/data/default-user/{characters,worlds}`。
- 上游只读参考件：`_tmp_review/dsh-tavern/`（**仅用于取接口与应然，不得复制代码**）。

---

## 8. 实践修订记录

- **2026-09-22 v0.1**：建立草案。主人定调「**全部自持**」（不 fork 上游）。设计取向：**测量优先于可玩**——装配单先于 UI（UI 先行会逼出事后改造，§5.20 规则 5 的事故族）。
- **2026-09-22 v0.2（实现落地 + 实践回修）**：
  1. **`description` 必须进上下文**（实现期实测修正）：原实现只注入 `personality`，而 ST 里
     `description` 才是**主定义**。实测：`default_Seraphina`（description 2844 字）装配出来只有
     1146 字——**主定义整段丢失**。已改为 description(99) / persona(100) / system_prompt(98) /
     scenario(90) / 对话样例(45) 五段分级注入。
  2. **V3 卡布局回落**（真数据暴露）：主人的 **57/58 张是 `chara_card_v3`，其中 44 张顶层字段全空**，
     内容在 `data.*`。只读顶层会把 44 张卡读成空角色。已加回落，并补 `data.character_book`
     （卡内世界书，57 张有）与 `alternate_greetings` 开场回落。
  3. **A5 抓到自引入回归**：`fromSt` 从 `data.*` 回落后，首版 `toSt` 仍一律写顶层 ⇒ 导出会把
     **空开场白覆盖成替补开场**（等于篡改主人的卡）。已改为「只回写变化过的字段，且写回其被读出位置」。
     **这是判据先行的收益：测试拦住了我自己。**
  4. **世界书边界显式化**：ST 有 20 种字段（递归/冷却/延迟/按角色字段匹配/宏出口/分组评分）本层
     不建模 ⇒ 从「未建模字段」改为**逐项带理由的登记表**，并区分「已知不做」与「未知可疑」。
  5. **面板走宿主而非自建**：`dsh-panel` 是本仓既有的面板宿主（主人 2026-09-12 定调「前端统一走这个宿主」），
     故贡献一份声明而非自写 webServer 界面；`panel` 作为**可选服务**用 `ctx.get` 取，缺席不影响工具面。
  6. **共享回合层**（A7 的落地）：工具与面板共用一个 `runTurn`；补全器以回调注入 ⇒ 整条回路
     **可离线用假补全器测试**，验收脚本因此能一条命令跑完。

---

## 9. 未决问题

1. ~~**命名**~~：定为 `dsh-dream-tavern`（梦境酒馆）。
2. ~~**卡格式**~~：**ST PNG（`chara` tEXt）+ JSON 双读**已实现；写回策略已修正为「只改被改过的字段」。
3. ~~**UI 范围**~~：走 `dsh-panel` 声明式面板（form/table/text/metrics/actions），自建 webServer 界面不做。
4. **状态模型**：当前为**自由 JSON**（结算 Agent 输出对象）。是否升级为可校验 schema 待定——
   自由 JSON 兼容性好但不可验证；迁移期间先保兼容。
5. **世界书规模上限**：单本最大 871 条已验证可导入；**触发匹配的成本与「按需读取」策略**待量化。
6. **A4 的定量化**：需要多轮真实采样，才能判「只追加 ⇒ 命中率不塌」。
7. **研究命题的最终形态**：注入位置/近因（`t-581d38e7`）+ 上下文质量四维 + 缓存命中，三条线如何合成一个可发表的实验设计（**卡点在设计不在代码**）。
